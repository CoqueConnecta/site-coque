# Backlog - Site Coque Connecta

Backlog de funcionalidades futuras e decisões adiadas.

Registrar aqui apenas o que ainda nao foi implementado.
Decisoes concluidas ficam documentadas em `docs/project-context.md`.

## Concluidas recentemente (remover apos proxima retrospectiva)

- ✅ Firebase Storage ativado (plano Blaze) — upload de imagens diretamente pelo painel admin
- ✅ Migracao de imagens locais (`/public/`) para Firebase Storage
- ✅ Biblioteca de imagens no admin carregada de `media/library` em tempo real
- ✅ Deploy migrado para Vercel (substituiu GitHub Pages + Render)
- ✅ Branch protection em `main` + workflow feature→staging→main via PR
- ✅ URL de staging: `staging.coqueconnecta.ong.br`

## Pendentes

(vazio — adicionar aqui as proximas features quando priorizadas)
