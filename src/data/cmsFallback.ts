// cmsFallback.ts — legacy v2 fallback, no longer used in v3.
// Kept as empty module to avoid import errors from any remaining references.
export const cmsFallbackByLanguage = { pt: {}, en: {} } as const;
